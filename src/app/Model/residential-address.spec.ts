import { ResidentialAddress } from './residential-address';

describe('ResidentialAddress', () => {
  it('should create an instance', () => {
    expect(new ResidentialAddress()).toBeTruthy();
  });
});
